# 직관 노트 (Intuition Note)

AI 코딩 에이전트가 이 저장소에서 작업할 때 따르는 지침.

---

## 1. 이 제품이 무엇인가 — 먼저 읽을 것

> ⚠️ `MVP.pdf`와 `직관_노트.pdf`를 표면적으로 읽으면 **반드시 오독한다.**
> 아래 정의가 팀이 합의한 최종 정의이고, 문서보다 우선한다.

### 정의

**사용자가 지금 고민 중인 선택지를 두고, 자신의 직관 — 즉 언어와 비언어 신호가 실제로 어떻게 반응하고 있는지 — 를 그대로 비춰주는 도구.**

### 흔한 오독 (하지 말 것)

| ❌ 오독 | ⭕ 실제 |
|---|---|
| "언어와 비언어의 **일치/불일치**를 판정한다" | 비교 대상은 "말 vs 몸"이 아니다. 둘 다 **고민 대상에 대한 직관의 반응**을 보여주는 재료다 |
| "직관에 데이터라는 **증거를 붙여** 이성과 싸우게 한다" | 이건 처방이 아니라 **질병 그 자체**다 (아래 실패 모드 ① 참조) |
| 직관_노트.pdf의 "일치도" | 여러 리포팅 방식 중 하나일 뿐, 제품의 정의가 아니다 |

### 가치 구조

```
AI의 대두 → 인간에게 희소해진 능력은 "결정력"
         → 결정력의 핵심은 메타인지
         → 메타인지는 원래 기르기 어렵다 (명상·저널링, 수년)
         → 데이터(음성/심박)가 그걸 즉각적·구체적으로 만든다  ← 우리의 기여
```

**이건 학습 서비스다. 물고기가 아니라 물고기 잡는 법.**
매 세션의 산출물은 "답"이 아니라 **더 나아진 결정자**다.

| 시간 | 얻는 것 | 사용자의 깨달음 |
|---|---|---|
| 1회 | **선호** | "고민 중인 줄 알았는데 사실 B에 마음이 가 있구나" |
| 반복 | **가치관** | "나는 이런 걸 중요하게 여기는 사람이구나" |
| 3개월~ | **확신** | "내 직관은 믿을 만하구나" |

→ 셋이 쌓여 **결정의 주체성**이 된다.

### 타깃이 겪는 두 실패 모드

1. **근거 아웃소싱 무한 루프** — 직관으로 이미 결정은 끝나 있고, 그 *근거*만 AI에게 대신 세워달라는 것. 마음에 드는 근거가 안 나오니 프롬프트를 계속 고쳐 쓴다. 근거를 백 개 받아도 구조적으로 해소되지 않는다. *(자기 판단의 **상태**를 모름)*
2. **추론 흐름의 상실** — AI의 추상적 설명을 못 따라가고 **결과만 소비**한다. 근거 기반 추론이 진행되지 않아 학습이 누적되지 않는다. *(자기 판단의 **과정**을 추적 못함)*

---

## 2. 절대 규칙

이 셋은 스타일 가이드가 아니라 **제품 메커니즘의 필수 조건**이다. 어기면 제품이 성립하지 않는다.

### ① 추천 금지

답을 주면 학습이 일어나지 않는다. 물고기를 주면 잡는 법을 못 배운다.
"추천 금지"는 AI의 겸손이나 윤리적 자제가 아니라 **교육학적 필수 조건**이다.

### ② 결론·라벨 단정 금지

```
❌ "B가 더 맞는 것 같아요"        → 결정 대행
❌ "A에서 불안이 감지됩니다"       → 라벨 단정. 결정 대행과 동일하다
❌ "~때문에 긴장하신 것 같아요"    → 원인 추정
⭕ "A를 말할 때 심박이 18% 올랐고 목소리는 낮아졌어요"  → 관찰
```

감정 라벨을 붙이는 순간 실패 모드 ②(결과만 소비)를 그대로 재생산한다.
**해석은 항상 사용자가 한다.**

### ③ 산파술 — 알지만 말하지 않고 이끈다

내부적으로는 판정한다(`lean: B, confidence: 0.72`). 하지만 **그걸 발화하지 않는다.**
대신 그 방향으로 **주의를 이끄는 질문**을 만들어 사용자가 스스로 결론에 도달하게 한다.

> **유도는 관찰이 아니라 질문에서 일어난다.**
> 관찰은 데이터 번역일 뿐이고, 질문의 *방향*이 우리가 쥔 유일한 핸들이다.
> — **비대칭 질문 원칙**: 신호가 강한 쪽으로 질문을 향한다.

---

## 3. 핵심 설계 — 두 축의 분리

**가장 자주 혼동되는 지점이다. 섞지 말 것.**

```
Δ(A, B)            →  선호   "어느 쪽에 마음이 가 있는가"
(A,B) vs default   →  상태   "지금 이 사람은 어떤 상태에서 이 기록을 남겼는가"
```

- **default는 선호 신호가 아니다.** 선호는 오직 A와 B의 차이에서 나온다.
- default는 그 차이를 **어떤 조건에서 읽어야 하는지 알려주는 프레임**이다.
- 같은 Δ(A,B)라도 `calm` 상태에서 나온 것과 `aroused` 상태에서 나온 것은 신뢰도가 다르다.
- 고민 중이라는 건 A와 B **둘 다** 긴장을 유발한다는 뜻이므로, default 없이 A↔B만 보면 노이즈를 신호로 오독한다.

### 판정 (`[4]` 단계) — 두 갈래 독립 계산

**선호 판정** `Δ(A,B)`
| 값 | 의미 |
|---|---|
| `lean_A` / `lean_B` | 한쪽으로 뚜렷 |
| `no_lean` | 차이 미미 — 진짜 팽팽하거나, 아직 안 익은 고민 |
| `contradictory` | 음성과 심박이 서로 반대 방향 |

**상태 판정** `평균 vs default`
| 값 | 의미 |
|---|---|
| `calm` | 평소 수준 — 선호 신호 신뢰도 높음 |
| `aroused` | 전반적 각성/긴장 — 이 고민 자체가 무겁다 |
| `flat` | 평소보다 전반적으로 낮음 — 피로/무기력/체념 |

상태가 `aroused`면 선호 판정의 `confidence`를 낮춰 표기한다.

### 리포트 순서 — 상태가 먼저

상태가 프레임이므로 먼저 나와야 한다.

```
① 상태 고지  "오늘은 평소보다 전반적으로 긴장이 높은 상태에서 기록하셨어요"
② 선호 관찰  "그 안에서, A를 말할 때와 B를 말할 때는 이렇게 달랐습니다..."
③ 신체 태깅  "A를 말할 때 몸에서 뭐가 느껴지셨어요?"        [가슴답답][손떨림][설렘][찜찜]
④ 자기 진술  "지금은 어느 쪽에 마음이 더 가 있는 것 같으세요?"  ★사용자가 자기 입으로 결론
```

①이 있어야 사용자가 이번 기록을 얼마나 믿을지 스스로 판단할 수 있다 — 그 판단 자체가 메타인지 훈련이다.
④가 결정적이다. 사용자가 자기 입으로 말하면 **우리 결정이 아니라 자기 결정**이 되고, 이 진술이 나중에 회고의 기준점이 된다.

---

## 4. 파이프라인 골조

**전체 골조를 먼저 세우고 각 단계에서 다듬는다.** 단계별 입출력 계약을 고정하면 안쪽 구현은 통째로 갈아끼울 수 있다.

```
[0] 고민 등록      in: 텍스트/음성 한 문장
    LLM: 선택지 추출 + 가치축 추출 + 상상 프롬프트 생성
                   out: Decision{title, options[], value_axis}
        ↓
[1] 캡처 (브라우저) in: Decision
    상상 15초 → 카메라 ROI RGB 시계열 / 발화 10초 → 오디오 + STT
                   out: Capture{rgb_series, audio, transcript}
        ↓
[2] 특징 추출       음성: openSMILE eGeMAPS(88) + Librosa
    (FastAPI)      심박: rPPG (detrend→bandpass 0.7~4Hz→FFT)
                   out: Features{voice{}, hr{bpm, confidence}}
        ↓
[3] 상대화 ★절대치 아님
    선호축: Δ(A,B)  /  상태축: (A,B) vs default
                   out: Delta{preference{}, state{}}
        ↓
[4] 판정           규칙 기반 (MVP)  ← 나중에 학습 모델로 교체
                   out: Verdict{lean, confidence, state}
        ↓
[5] 리포트 (Solar)  ★추천·결론·라벨 금지. 관찰 + 질문만
                   out: Report{state_note, observations[], question}
        ↓
[6] 사용자 태깅     out: Annotation{body_tags[], self_lean}
        ↓
[7] 저장 (SQLite)
        ↓
[8] 축적 뷰        가치관 지도 / 회고 루프 (1주·3개월·6개월)
```

**교체 지점 (계약만 지키면 갈아끼우기 가능)**
- `[2]` rPPG → 웨어러블 HRV
- `[3]` `baseline_source: "session_neutral" | "accumulated" | "continuous_wearable"`
- `[4]` 규칙 → 학습 모델

---

## 5. 세션 흐름 (약 80초)

```
[중립 앵커]  15초  "화면의 점을 편하게 바라보세요"   → default 심박 + rPPG 워밍업
             15초  "오늘 아침에 뭐 하셨어요?"       → default 음성
[고민 입력]        한 문장                         → LLM이 선택지 + 가치축 추출
[선택지 ①]  15초  "그 미래를 상상해보세요" (정지)   → 심박 ★
             10초  "그 느낌을 말해주세요"           → 음성 ★    ※ 제시 순서 랜덤
[선택지 ②]  동일
[리포트]           상태 → 선호 → 태깅 → 자기 진술
```

**중립에도 `imagine` 구간이 있어야 한다.** 심박은 정지 구간에서만 측정되므로, 중립에 발화만 두면 default 심박이 없어 상태축에서 심박이 통째로 빠진다.

**왜 심박을 "말하는 중"이 아니라 "상상 중"에 재는가**
말하면 얼굴 근육이 움직여 rPPG 신호가 깨진다. 상상 구간(정지)에서 재면 신호 품질이 크게 좋아지고, 상상→발화 순서가 심리적으로도 자연스러우며(prospection이 신체 반응을 먼저 유발), 15초 상상한 뒤라 백지 공포도 준다.

**중립 앵커는 고민과 무관한 발화여야 한다.** 고민을 말하는 순간 이미 긴장 상태다.

---

## 6. 기술 스택

| 영역 | 선택 | 근거 |
|---|---|---|
| 프론트 | React + Vite (**웹**) | 모바일은 시간상 부적합. `getUserMedia`로 오디오·비디오 동시 |
| 얼굴 ROI | MediaPipe Face Mesh (브라우저) | **RGB 평균 시계열 숫자만 서버로 전송** — 영상 원본이 서버에 안 올라간다 |
| STT | **faster-whisper** (백엔드) | 브라우저 Web Speech는 오디오를 **구글 서버로 보낸다** — 아래 참조 |
| 백엔드 | FastAPI + numpy/scipy | rPPG 신호처리, openSMILE, librosa |
| 음성 특징 | **openSMILE** (eGeMAPS 88) | Essentia에는 jitter/shimmer/HNR이 **없다** → 대체 불가 (`docs/OPEN_QUESTIONS.md` Q2) |
| LLM | **Upstage Solar** | 대회 가점 +5. `[0]` 파싱, `[5]` 리포트 |
| DB | SQLite | MVP 충분. 스키마는 확장 가능하게 |

**프라이버시는 기능이 아니라 전제다.** 몸이 흘리는 신호는 가장 사적인 데이터이고, 그 신뢰가 없으면 사용자는 진짜 고민을 말하지 않는다. 영상 원본을 서버에 보내지 않는 설계를 지킬 것.

> ⚠️ **"브라우저 API = 로컬 처리"가 아니다.**
> 초기 설계는 STT에 브라우저 Web Speech API를 썼다. MediaPipe처럼 브라우저 안에서
> 처리될 거라 **확인 없이 가정했는데**, Chrome은 오디오를 구글 서버로 보내 인식한다.
> 영상은 안 보내려고 공들여놓고 음성은 밝히지도 않은 채 제3자에게 보내고 있었다.
> 걷어내고 백엔드 `faster-whisper`로 바꿨다 (`docs/OPEN_QUESTIONS.md` Q7).
>
> **새 브라우저 API를 넣을 때는 데이터가 어디로 가는지 먼저 확인할 것.**

---

## 7. 저장소 구조

```
frontend/          React + Vite    — 자세한 주의사항은 frontend/README.md
backend/           FastAPI         — 자세한 주의사항은 backend/README.md
docs/
  CONTRACTS.md        ★파이프라인 계약. 스키마의 단일 진실 소스
  OPEN_QUESTIONS.md   의도적으로 미룬 사안 — 새 의문이 생기면 여기 추가
MVP.pdf             참고용 (정의는 이 문서가 우선)
직관_노트.pdf        참고용 (정의는 이 문서가 우선)
```

**계약을 고칠 때는 세 곳을 함께 고친다**: `docs/CONTRACTS.md` → `backend/app/schemas/*.py` → `frontend/src/types/contracts.ts`

---

## 8. 팀 컨벤션

### API 응답 형식
```json
{ "ok": true, "data": {}, "error": null }
```
실패 시 `ok: false`, `data: null`, `error: { "code": "...", "message": "..." }`

### 커밋
`feat|fix|docs|refactor(scope): 제목` — 100자 이내, 기능 단위로 작게 자주

### 브랜치
기능 단위 브랜치 → PR로 머지

### Git 권한 ⚠️
- **에이전트는 `git commit`, `git push`를 스스로 판단해 실행하지 않는다.**
- 필요하다고 판단되면 사람에게 먼저 제안하고, **명시적으로 지시받았을 때만** 실행한다.

### 에러 처리
raw traceback을 클라이언트에 노출하지 않는다. 4xx=클라이언트 오류, 5xx=서버 오류.

### 보안
API 키·DB 접속정보는 `.env`에만. 절대 커밋 금지.
`.claude/`, `CLAUDE.md`는 저장소에 커밋한다 (대회 심사 반영 대상).

---

## 9. 작업 방식

- **큰 기능은 plan 먼저** — 바로 코딩하지 말고 설계 검토 → 승인 후 구현
- **의문은 기록** — 지금 결정할 수 없는 건 `docs/OPEN_QUESTIONS.md`에 남기고 골자를 우선한다
- **AI 활용 기록** — 세션마다 무엇을 AI와 함께 했는지 한 줄씩 (데모/회고/심사용)
- **성공 지표는 리텐션이 아니라 졸업** — 사용자가 앱 없이 자기를 읽게 되는 것. 훈련은 원래 졸업을 전제로 한다
